一.先理解DFS和BFS的区别
  假设有这样的一个图：
        1
      /   \
     2     3
    / \   / \
   4   5 6   7
  DFS：从一条路走到底
  从1 开始：
    1 → 2 → 4
        ↓
        5
    → 3 → 6
          ↓
          7
  可能的访问过程是：
    1 2 4 5 3 6 7

  BFS一层一层访问
  从1 开始
    1
  第二层
    2 3
  第三层
    4 5 6 7
  可能的访问过程
    1 2 3 4 5 6 7
二.BFS最重要的工具：Queue
  | 队列的特点是，先进先出
  例如：
    Queue：
    [1]
  把1拿出来： 然后发现1有的下一层有两个邻居 2 3
  2 3 加入队列：
    [2, 3]
  拿出2
    [3]
  发现2，的下一层有4 5 加入队列
    [3, 4, 5]
  拿出3
    [4, 5]
  发现3 的下一层有6 7 加入队列
    [4, 5, 6, 7]
  于是自然形成了
    1
    ↓
    2 3
    ↓
    4 5 6 7
三.BFS的代码模板
  最基本的BFS：
    queue<int> q;

    q.push(1);

    while(!q.empty()){
        int cur = q.front();
        q.pop();

        // 处理 cur

        // 把 cur 的邻居加入 Queue
    }
四.visited
  在DFS网络题里一直强调
    | 为什么要记录visited
  在BFS里面也需要 visited
    vector<bool> visited(n, false);
  然后
    visited[i] = true;
    q.push(1);
  当准备把1的邻居加入队列时：
    if(!visited[nect])
    {
        visited[next] = true;
        q.push(next);
    }
  | 不是出队的时候才标记visited， 而是入队的时候就标记
