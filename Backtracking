一.回溯（Backtracking）
  为什么需要回溯：
  假设有
    1 2 3
  要求输出所有的排列
    123
    132
    213
    231
    312
    321
  如果用for循环
    for(...)
      for(...)
        for(...)
  三个数字三层循环
二.回溯思想
  由此可以
  先选
    1
  再选
    2
  最后选
    3
  形成一组答案、
    123
  然后删除
    3
  剩下
    1 2
  重新走新的路径
  整个流程就是 选择-> 递归-> 撤销选择（Backtrack）
三.重要模板
  void dfs(...)
  {
      if(结束条件)
      {
          保存答案;
          return;
      }

      for(...)
      {
          做选择;

          dfs(...);

          撤销选择;
      }
  }
核心三步就是
  push_back()
  dfs()
  pop_back()
四.画递归树
            []
         /   |   \
       1     2     3
      / \   / \   / \
    2   3   1  3  1  2
    |   |   |  |  |  |
    3   2   3  1  2  1
  所以回溯其实就是：
    DFS（深度优先搜索）
五.path是什么
  几乎所有回溯题都有
    vector<int> path;
  path表示已经选择的答案
六.isited数组
  很多排列题都会出现
    vector<bool> used(n, false);
  作用是
    防止重复使用
  例如
    1 2 3
  如果已经用了1
    used[0] = true
  表示这一层里面第0位的1已经不能再选了
七.经典框架
vector<vector<int>> ans;
vector<int> path;
vector<bool> used;

void dfs(vector<int>& nums)
{
    if(path.size()==nums.size())
    {
        ans.push_back(path);
        return;
    }

    for(int i=0;i<nums.size();i++)
    {
        if(used[i])
            continue;

        used[i]=true;

        path.push_back(nums[i]);

        dfs(nums);

        path.pop_back();

        used[i]=false;
    }
}
