一.DFS（深度优先搜索）
  以迷宫举例：
    s . .
    # # .
    . . E
  s是起点，E是终点
  DFS会：
    一直走
      ↓
   能走继续走
      ↓
    走到底
      ↓
    发现死路
      ↓
    返回
      ↓
  尝试另一条路

二.DFS经典模板

  void dfs(状态)
  {
      if(结束条件)
      {
          return;
      }

      做选择;

      dfs(下一状态);

      撤销选择（有需要的话）
  }

举个简单例子，输出：
    1
    2
    3
DFS：
    void dfs(int x)
    {
        cout << x << endl;

        if(x == 3)
            return;

        dfs(x + 1);
    }
执行过程：
    dfs(1)
      ↓
    输出1
      ↓
    dfs(2)
      ↓
    输出2
      ↓
    dfs(3)
      ↓
    输出3
      ↓
    return
      ↓
    return
      ↓
    return
递归栈：
    dfs(1)
     ↓
    dfs(2)
     ↓
    dfs(3)
然后一层一层退出
  
