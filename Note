一.DFS的统计
  现在我们知道
    int dfs(...)
  可以返回一个数字，而这个数字可以是
    . 岛屿面积
    . 连通区域大小
    . 路径数量
    . 节点数量
    . 某个区域满足条件的数量
二.DFS的返回值其实可以理解成【我这棵树贡献了多少】
  例如
    return 1
      + dfs(i+1,j)
      + dfs(i-1,j)
      + dfs(i,j-1)
      + dfs(i,j+1);
  将整颗树的值从深层递归返回出来
三.一共有多少颗树
  如果题目问：
    [一共有多少区域]
  这对应LeetCode 200 Number of Island
  通常模版是
    int count = 0;

    for(...) {
    for(...) {

        if(发现一个新的区域) {
            count++;
            dfs(...);
            }
        }
    }
四.区分非常像的三种情况
类型 A：只遍历
    void dfs(...)
  目的：
    | 把所有连通节点访问一遍
  
类型 B：统计一个区域
    int dfs(...)
  目的：
    | 返回这个区域有多少东西
  例如：
    return 1 + 四个方向
  
类型 C：判断能不能到达
  例如：
    bool dfs(...)
