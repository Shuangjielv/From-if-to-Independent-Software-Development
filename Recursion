一.什么是递归
  递归(Recursion) 就是：
    一个函数调用它自己
  例如：
    void hello()
    {
        cout << "Hello"<< endl;
        hello();
    }

  但是以上代码会产生无限递归的问题
  所以：
    递归一定要有结束条件（Base Case）
二.递归的组成
  任何递归，都必须包含两部分：
    1.结束条件（Base Case）
    2.递归调用（Recursive Call）
三.经典例子，计算阶乘
  数学定义：
    5！= 5 x 4 x 3 x 2 x 1
  对应代码：
    int factorial(int n)
    {
        if (n == 1)
          return 1;

        return n * factorial(n - 1);
    }

    cout << factoria(5);
  输出：
    120
四.理解返回过程（递归最重要的部分）
  以阶乘举例  
    factoria(5)  
  其实会展开成
    5 * factoria（4）
  而
    factoria(4) 
  又等于
    4 * factoria(3)
  依次类推得到
    5 * 4 * 3 * 2 * factoria（1）
  到这里 
    factoria(1)
  返回
    1

  于是开始一层层返回
    2 x 1 = 2
    3 x 2 = 6
    4 x 6 = 24
    5 x 24 = 120
  先不断向下调用，达到结束条件后，再一层一层返回结构
