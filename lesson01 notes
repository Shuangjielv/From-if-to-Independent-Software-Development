1.CPU的职责是什么？
根据windows的指令执行操作，但是cpu并不认识自己打开了什么，cpu只是在执行当前内存中的机器指令
2.为什么程序不能直接在SSD上运行？
因为SSD就像是一个仓库，SSD是存储本地数据的地方，SSD负责长期保存数据，但速度远远慢于RAM
3.RAM和SSD最大的区别是什么？
RAM本身是一个存储运行文件的地方，将文件从SSD送到RAM再由CPU执行能极大提高效率，在系统关闭后RAM没有存储数据功能
4.为什么要Windows操作系统？如果没有操作系统会发生什么？
操作系统是统筹CPU和RAM和SSD运作的终端，先发送指令给SSD将文件送到RAM再由CPU执行.如果没有Windows，会出现CPU在RAM上找不到执行文件，RAM上没有执行文件，SSD里面的文件检索到了但没法启动，所以Windows负责调度指令的先后，负责统筹不同文件位于不同的位置执行指令

5.CPU永远只能直接执行RAM中的程序，如果RAM已经满了，Windows会将暂时不用的数据搬回SSD的一个临时文件夹（virtual Memory）虚拟内存，由于需要反复在RAM和SSD之间搬运数据，所以会出现电脑响应缓慢的情况
